# Local development: leave empty to use the Vite proxy (/api -> http://127.0.0.1:5050).
VITE_API_URL=

# Separate frontend deployment (optional): set this in Vercel env vars instead of using client/vercel.json rewrites.
# VITE_API_URL=https://blood-bank-management-system-i7pj.onrender.com/api
