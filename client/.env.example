# Local development uses the Vite /api proxy, so this can remain empty locally.
# In Vercel, set this to the deployed backend API URL, including /api.
VITE_API_URL=https://your-backend-domain.vercel.app/api
