{
  "name": "blood-bank-management-mern",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "concurrently \"pnpm --dir server dev\" \"pnpm --dir client dev\"",
    "build": "pnpm --dir client build",
    "start": "pnpm --dir server start",
    "test": "pnpm --dir server test",
    "migrate:sqlite": "pnpm --dir server migrate:sqlite",
    "seed:admin": "pnpm --dir server seed:admin",
    "seed:demo": "pnpm --dir server seed:demo",
    "seed:profiles": "pnpm --dir server seed:profiles"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
